# springboot-library-management-system
Spring Boot REST API based Library Management System built with Java 21, Spring JDBC, MySQL, and layered architecture (Controller → Service → Repository). Features include member registration, book management, borrow records, fine calculation, and pagination.
